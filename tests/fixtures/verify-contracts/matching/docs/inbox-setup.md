# Inbox Setup Guide

Use `http://<your-mac-ip>:7777/inbox` in your shortcut.

```bash
curl -X POST http://localhost:7777/inbox \
  -H 'Content-Type: application/json' \
  -d '{"url":"https://example.com"}'
```
