# Tag: self-improving-agents

- `INS-260627-A5D1` Don't let one reflector gate your agent's learning; add quality gates, ensemble reflection, and a fallback route when reflection degrades.
- `INS-260627-E0C0` Treat agent optimization as a multi-dimensional allocation problem orchestrated by a meta-layer, not a single knob like context.
