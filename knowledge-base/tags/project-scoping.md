# Tag: project-scoping

- `INS-260514-2C6C` Build a minimal core + project-specific extensions — not a kitchen-sink globally-enabled toolkit. Most globally-installed skills are never used and just pollute context.
