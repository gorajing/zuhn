# Tag: software-factory

- `INS-260501-EC31` Tan runs 10-15 parallel Claude Code sessions per day, each in its own work-tree, shipping 10-50 PRs daily as orchestrator rather than implementer.
- `INS-260501-3502` Spec + tests = the human's job. Implementation + iteration to passing = the agent's job. Some repos already contain zero handwritten code.
