# Tag: recursion

- `INS-260625-AF2A` If the harness hardcodes the decomposition, it's a map-reduce with LLM calls; if the model picks the decomposition, it's a genuine recursive agent.
- `INS-260501-94AC` Same weights applied N times = same compute as N-layer stack with 1/N the parameters.
- `INS-260325-D4F6` In infinite or self-similar systems, a subset can contain everything the whole contains.
