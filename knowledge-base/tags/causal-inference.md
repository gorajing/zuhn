# Tag: causal-inference

- `INS-260505-33E6` If you generate 'less aggressive' versions of public comments, the LLM also makes them more agreeable — residualize or you'll attribute the wrong cause.
- `INS-260605-C37E` When your injector detects a relevant answer, sometimes inject nothing — then compare success rates between injected and would-have-injected cohorts.
- `INS-260403-27AA` Change only one variable per A/B test to maintain causal attribution.
