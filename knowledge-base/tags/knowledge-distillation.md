# Tag: knowledge-distillation

- `INS-260605-BF58` SelfFlow trains a heavily-noised 'student' and a stable lightly-noised 'teacher' (an EMA of the student) so one model learns generation and representation jointly — no external encoder, scales as you scale the model.
- `INS-260330-52F6` Knowledge distillation works because student models learn probability distributions, not just correct answers
