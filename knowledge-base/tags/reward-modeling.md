# Tag: reward-modeling

- `INS-260628-1175` A GPT-4o-mini self-verified debate ensemble beat o1/o3-mini on expert QA grading at under a third the cost and latency.
- `INS-260628-DFA3` Before you iterate your app against a judge, judge the judge — raw LLM-as-judge calls hallucinate, are uncalibrated, and flip on input order.
- `INS-260627-DF51` SFT-era fine-tuning died on the difficulty of creating labeled examples; environments sidestep this by asking only for a way to score outcomes, so data is created on the fly during training.
