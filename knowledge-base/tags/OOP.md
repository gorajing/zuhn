# Tag: OOP

- `INS-260329-BA12` Inheritance (Chef is-a subclass of Person) seems elegant but creates rigid hierarchies — in practice, composition ('has-a') produces more flexible systems.
- `INS-260329-0432` Classes let you create custom data types that model real-world entities — the core value of OOP is domain modeling, not inheritance.
- `INS-260329-1610` Static attributes reveal that classes are first-class entities with their own state, not merely blueprints for objects.
