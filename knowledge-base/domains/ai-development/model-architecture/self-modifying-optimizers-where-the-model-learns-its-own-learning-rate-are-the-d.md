---
id: INS-260501-FDF0
domain: ai-development
topic: model-architecture
title: >-
  Self-modifying optimizers — where the model learns its own learning rate — are
  the deeper insight in Hope
actionability: inspiration
confidence: medium
shelf_life: evergreen
status: active
tags:
  - meta-learning
  - self-modification
  - learned-optimizer
  - self-modifying-titans
sources:
  - type: youtube
    title: The End of Frozen LLMs? (Google’s Hope Explained)
    author: AI Papers Academy
    url: 'https://youtu.be/VTQR9n3aqNU'
date_extracted: '2026-05-01'
last_accessed: null
access_count: 0
indexed: true
embedded: true
embedding_model: nomic-embed-text
resolutions:
  one_line: >-
    Hope's models don't just learn from input — they learn how to learn from
    input, generating their own learning rates and decay factors per situation.
  standard: >-
    The buried lede in Hope's Self-Modifying Titans: the optimizer that updates
    memory is itself a learned module. The model dynamically generates its own
    learning rate (how aggressively to incorporate new information) and decay
    factor (how much past memory to retain or forget) using dedicated
    sub-modules — and those sub-modules are themselves self-adaptive. This is
    meta-learning at the architectural level: the model learns how to update
    itself based on the situation it's in.


    The practical implication: rather than picking a global learning rate at
    training time, models can decide on the fly that a particular interaction
    merits aggressive memorization (rare, important content) or rapid forgetting
    (transient noise). Each module decides what to store based only on the input
    it sees, with the initial states aligned to global task via training. For
    Zuhn editorially: this is a research-frontier marker — when self-modifying
    optimizers transition from paper-stage to production architecture (currently
    they're at the 'outperforms Transformer on small benchmarks' stage), it will
    be one of the architectural pieces that unlocks Hassabis's
    continual-learning bottleneck.
stance: >-
  The most novel idea in the Hope architecture is not multi-frequency updates
  but that the optimizer itself (learning rate, decay factor) is a learned
  module, dynamically generated by the model based on what it's seeing — true
  meta-learning.
related:
  - INS-260323-584D
  - INS-260322-3616
  - INS-260321-8C35
  - INS-260327-E07C
  - INS-260328-3446
  - INS-260329-3D3E
  - INS-260330-3D7B
  - INS-260330-DF2C
  - INS-260405-227C
  - INS-260320-96C9
---
The buried lede in Hope's Self-Modifying Titans: the optimizer that updates memory is itself a learned module. The model dynamically generates its own learning rate (how aggressively to incorporate new information) and decay factor (how much past memory to retain or forget) using dedicated sub-modules — and those sub-modules are themselves self-adaptive. This is meta-learning at the architectural level: the model learns how to update itself based on the situation it's in.

The practical implication: rather than picking a global learning rate at training time, models can decide on the fly that a particular interaction merits aggressive memorization (rare, important content) or rapid forgetting (transient noise). Each module decides what to store based only on the input it sees, with the initial states aligned to global task via training. For Zuhn editorially: this is a research-frontier marker — when self-modifying optimizers transition from paper-stage to production architecture (currently they're at the 'outperforms Transformer on small benchmarks' stage), it will be one of the architectural pieces that unlocks Hassabis's continual-learning bottleneck.
